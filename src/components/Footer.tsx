
import React from 'react';
import Footer from './footer/Footer';

// This is a simple re-export component to maintain backward compatibility
const FooterWrapper = () => {
  return <Footer />;
};

export default FooterWrapper;
